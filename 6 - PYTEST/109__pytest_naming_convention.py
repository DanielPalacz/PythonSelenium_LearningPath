import pytest


# when we run all tests by teh pytest command (or py.test)
# 1)
# only files with 'test' inside the filenames will be running
# 2)
# only methods starting with 'test_' will be running
